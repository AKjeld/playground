#include <iostream>
#// INCLUDES
#include <fstream>
#include <unordered_map>
#include <vector>
#include <sstream>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cerr << endl
         << "finished in " 
         << clock() * 1.0 / CLOCKS_PER_SEC 
         << " sec" 
         << endl;
    return 0;
}
